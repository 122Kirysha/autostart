# autostart
Автоматически запускает тг обходник тг(если он есть) запрет и дискорд (САМИМ НАДО ВСТАВИТЬ ПУТИ)
python -m PyInstaller --onefile --windowed --name "DoubleClapOpener" "clap_opener.py" в коде чтобы создать exe
